{
  "version": "1.0.0",
  "name": "<%= name %>",
  "private": true,
  "scripts": {
    "build": "webpack --env=prod --progress --colors --hide-modules && gulp deploy",
    "dev": "webpack --env=dev --progress --colors --hide-modules",
    "upload": "gulp",
    "test": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/xrm": "^8.2.4",
    "gulp": "^3.9.1",
    "gulp-cached": "^1.1.1",
    "gulp-webresource": "^1.3.0",
    "source-map-loader": "^0.2.1",
    "ts-loader": "^2.0.3",
    "typescript": "^2.3.2",
    "webpack": "^2.5.1",
    "xrm-webapi": "^2.0.5"
  }
}
