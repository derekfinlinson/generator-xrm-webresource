import { WebApi } from "xrm-webapi";

export class <%= filename %> {
    static onLoad() {
        // Define on load events

        // Add on change events
        
        // Add on save events
    }
}